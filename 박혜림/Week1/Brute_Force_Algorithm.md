# Brute Force Algorithm
- Brute Force Algorithm 은 가능한 모든 경우의 수를 직접 탐색하여 답을 찾는 완전 탐색 알고리즘이다. 구현이 단순하여 다중 for문과 조건문(if)으로 표현되는 경우가 많고, 문제에 따라 재귀나 이진수 비트마스킹으로 구현하기도 한다.
- 이 알고리즘은 항상 정답을 보장하지만 입력 크기가 커질수록 시간복잡도가 O(n^2), O(2^n), O(n!) 수준으로 급격히 증가하기 때문에 입력이 작은 경우나 다른 알고리즘의 정확성 검증 용도로 주로 사용한다.
