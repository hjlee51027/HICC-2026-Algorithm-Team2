from itertools import combinations


L, C = map(int, input().split())
words = sorted(list(input().split()))
vowels = ['a', 'e', 'i', 'o', 'u']

for c in combinations(words, L):
    k = len(set(c) & set(vowels))

    if (k >= 1) & (L - k >= 2):
        print("".join(c))
