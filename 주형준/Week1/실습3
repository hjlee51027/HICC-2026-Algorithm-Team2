from itertools import permutations


N = int(input()
nums = list(map(int, input().split()))
max_sum = 0

for p in permutations(nums):
    sum = 0

    for i in range(N - 1):
        sum += abs(p[i] - p[i + 1])

    if sum > max_sum:
        max_sum = sum

print(max_sum)
